spring.application.name=LearnSpringBoot
server.port=8888
#spring.profiles.active=${SPRING_PROFILE:dev}

#test.db.url=${DB_URL:testUrl}
#test.db.username=username
#test.db.password=password
#test.connection.max.limit=${MAX_CONNECTION:2}
#app.database.type=H2

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/testing_db
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=org.postgresql.Driver
test.connection.max.limit=${MAX_CONNECTION:2}

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT config 3 months
jwt.secret=${JWT_SECRET:685ecf63d1414d9320ad2d494a355702675c96b2cb5d1f05b2e968b0712d10ae}
jwt.expiration=${JWT_EXPIRATION:7776000}


